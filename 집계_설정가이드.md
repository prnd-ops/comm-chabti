# 차BTI 설문 집계 설정 가이드

설문 페이지에 **접속수 · 시작수 · 완료수 · 문항별 선택**을 자동으로 구글 시트에 쌓는 방법입니다.
개발 지식 없이 **약 5분**이면 끝나요. 순서대로만 따라오시면 됩니다.

---

## 준비물
- 구글 계정 (selah님 회사 계정이면 됩니다)

---

## 1단계 — 구글 시트 만들기
1. https://sheets.google.com 접속 → **빈 스프레드시트** 새로 만들기
2. 이름을 예: `차BTI 설문집계` 로 바꿔주세요.
   (시트 탭은 비워두셔도 됩니다. 코드가 자동으로 `이벤트로그`, `완료응답` 탭을 만들어요.)

## 2단계 — Apps Script 코드 붙여넣기
1. 그 시트 상단 메뉴에서 **확장 프로그램 → Apps Script** 클릭
2. 열린 편집기의 기존 내용(`function myFunction(){}`)을 **전부 지우고**,
   같은 폴더의 **`집계_AppsScript_Code.gs`** 파일 내용을 **통째로 복사해서 붙여넣기**
3. 💾 저장 (Ctrl/Cmd + S)

## 3단계 — 웹앱으로 배포하기
1. 편집기 오른쪽 위 파란 **배포 → 새 배포** 클릭
2. 톱니바퀴(⚙️) → **웹 앱** 선택
3. 설정을 이렇게 맞춰주세요:
   - **설명**: 아무거나 (예: 차BTI 집계)
   - **다음 사용자로 실행**: `나(본인)`
   - **액세스 권한이 있는 사용자**: **`모든 사용자`** ← ⚠️ 꼭 이걸로!
4. **배포** 클릭 → 권한 승인 창이 뜨면 본인 계정으로 **허용**
   (“Google에서 확인하지 않은 앱” 경고가 나오면 → **고급 → (안전하지 않음으로) 이동** → 허용.
    본인이 만든 스크립트라 안전합니다.)
5. 배포가 끝나면 나오는 **웹 앱 URL**을 복사하세요.
   `https://script.google.com/macros/s/........./exec` 형태입니다.

## 4단계 — 설문 HTML에 URL 연결하기
1. `차BTI_현재로컬변형_백업_20260629.html` 파일을 편집기로 열기
2. 아래 줄을 찾아서 (`Ctrl/Cmd + F` 로 `TRACK_ENDPOINT` 검색):
   ```js
   const TRACK_ENDPOINT="";
   ```
3. 따옴표 안에 3단계에서 복사한 URL을 붙여넣기:
   ```js
   const TRACK_ENDPOINT="https://script.google.com/macros/s/여기에_본인_URL/exec";
   ```
4. 저장. **끝!**

> 이 한 줄만 있으면 됩니다. 나중에 시트를 바꾸고 싶으면 이 URL만 갈아끼우면 돼요.

---

## 잘 되는지 확인
1. 설문 페이지를 열고 → 테스트를 **처음부터 끝까지** 한 번 진행
2. 구글 시트로 돌아가 보면:
   - **`이벤트로그`** 탭에 `view → start → answer(8줄) → complete` 행이 쌓입니다.
   - **`완료응답`** 탭에 유형코드 + Q1~Q8 선택이 한 줄로 남습니다.
3. (선택) 웹앱 URL을 브라우저 주소창에 그대로 붙여넣고 열면
   `{"접속수":1,"시작수":1,"완료수":1,"완료율":"100%"}` 같은 요약이 바로 보입니다.

---

## 쌓이는 데이터 읽는 법

### 이벤트로그 탭 (모든 행동 원본)
| 열 | 뜻 |
|---|---|
| 시각 | 행동이 일어난 시간 |
| 이벤트 | `view`(접속) `start`(시작) `answer`(문항응답) `complete`(완료) `budget`(예산대클릭) `share`(공유버튼) `click`(그 외 버튼클릭) |
| 방문자 | 같은 브라우저면 재방문해도 동일 ID |
| 세션 | 한 번의 접속(방문 회차) |
| 문항 | answer일 때 몇 번 문항인지 (1~8) |
| 선택 | `a` / `b` |
| 축 | 그 선택의 성향축 글자 (F/D/V/B/S/N/M/R) |
| 유형코드 | complete일 때 나온 16유형 (예: `FVSM`) |
| 선택지문구 | 실제로 고른 보기 텍스트 / `click`일 땐 버튼 이름 |

**결과페이지 버튼 클릭은 이렇게 남습니다** (이벤트=`click` 또는 전용 이벤트):
| 버튼 | 이벤트 | 구분 |
|---|---|---|
| 예산대 6개 | `budget` | `예산대` 열에 1~6 |
| ↗ 친구에게 공유하기 | `share` | — |
| 📖 16가지 모든 유형 보기 | `click` | 선택지문구 = `결과_모든유형보기` |
| 🔁 다시하기(하단) | `click` | 선택지문구 = `결과_다시하기` |
| 상단바 로고 / ↺ 다시하기 | `click` | 선택지문구 = `상단_로고` / `상단_다시하기` |

→ 특정 버튼 클릭 수는 `이벤트로그`에서 `선택지문구`로 세면 됩니다.
예) 공유 클릭 수: `=COUNTIF(이벤트로그!B:B,"share")`,
   모든유형보기 클릭 수: `=COUNTIF(이벤트로그!J:J,"결과_모든유형보기")`

### 완료응답 탭 (끝까지 한 사람만)
한 줄 = 한 명의 완료 응답. `유형코드`·**`유형명`(M열, 예: 가성비 드라이버)** + `Q1~Q8`에
문항별 a/b 선택이 들어있어 "어느 문항에서 어떤 선택이 많았나",
"어떤 유형이 많이 나왔나"를 바로 피벗테이블로 뽑을 수 있어요.

### 자주 쓰는 집계 (원하면 새 탭에 붙여넣기)
- **총 접속 수(세션 기준)**: `이벤트로그`에서 이벤트=`view`인 세션 개수
  `=COUNTA(UNIQUE(FILTER(이벤트로그!D:D, 이벤트로그!B:B="view")))`
- **완료 수**:
  `=COUNTA(UNIQUE(FILTER(이벤트로그!D:D, 이벤트로그!B:B="complete")))`
- **완료율**: 완료수 ÷ 접속수
- **유형별 분포**: `완료응답!M:M`(유형명)으로 피벗테이블 → 개수 (예: 가성비 드라이버 12명)
- **문항별 이탈**: `이벤트로그`에서 이벤트=`answer`, 문항별(1~8) 세션 수를 비교하면
  몇 번 문항에서 사람들이 빠져나가는지 보입니다.

---

## 자주 묻는 질문
- **개인정보 수집인가요?** 아니요. 이름·연락처 같은 건 전혀 안 받고, 익명 랜덤 ID와 선택값만 남습니다.
- **공짜인가요?** 네. 구글 시트 + Apps Script 무료 범위 안에서 충분합니다.
- **URL을 안 넣으면?** `TRACK_ENDPOINT`가 비어 있으면 트래킹 코드는 그냥 아무것도 안 합니다. (설문은 정상 작동)
- **코드를 고치면 재배포해야 하나요?** Apps Script를 수정하면 **배포 → 배포 관리 → (연필)편집 → 새 버전 → 배포**로 갱신하세요. URL은 그대로 유지됩니다.
